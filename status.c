#include <cs50.h>
#include <stdio.h>

int main( int argc , string argv[])
{
    if (argc != 3 )
{
    printf("error \n ");
    return 1;
}
printf("hello, %s\n", argv[1] );
}
// write echo $ ? in command
